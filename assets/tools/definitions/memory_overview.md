<!--
{
  "name": "memory_overview",
  "handler": "memory_overview",
  "description": "List dynamic memory files for the current scope.",
  "parameters": {
    "type": "object",
    "properties": {
      "project_slug": {"type": "string", "description": "Optional project scope."}
    }
  }
}
-->

# Tool: memory_overview

## Usage Hint
- Use this tool to inspect the available memory stores and indexes.
- Use it when deciding where prior research context may be stored before searching or reading files.

